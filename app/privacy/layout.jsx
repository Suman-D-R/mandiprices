import React from 'react';

export const metadata = {
  title: 'Policy | MandiPrices.in',
  description: 'Our privacy policy, terms of service, and data usage policy.',
};

export default function PolicyLayout({ children }) {
  return <>{children}</>;
}
